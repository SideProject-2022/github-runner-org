name: test checkout
on: push
jobs:
  build:
    runs-on: [self-hosted, Linux, X64]
    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: |
        echo Hello, world!
        ls -al
        echo "Org Level self-hosted runner test"